package tech.xigam.simpleauth.objects.absolute;

/**
 * Response codes for signing in to the dispatch server.
 */
public final class ResponseCodes {
    /* Success codes. */
    public static final int SUCCESS = 0;
    
    /* Failure codes. */
    public static final int INVALID_ACCOUNT = -201;
}
